# Dashboard Template Archive v0.01

This folder captures the early Beta dashboard template checkpoint.

- `page.tsx` is the archived dashboard UI file from `src/app/page.tsx`.
- `LogoSnoutBoard.svg` is the active logo asset at the time of the archive.
- Build label: `v0.01`.
- Archive date: May 13, 2026.

This archive is intended as a reference snapshot, not an active runtime path.
